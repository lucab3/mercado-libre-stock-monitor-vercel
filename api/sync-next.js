/**
 * Endpoint serverless para sincronización next de productos
 * Usa el código completo de src/api/sync-next.js
 */

const handleSyncNext = require('../src/api/sync-next');

// Exportar directamente la función del sync-next
module.exports = handleSyncNext;